# Weather Frontend

Minimal React + Vite frontend that calls the `weather-service` backend.

Install and run:

```powershell
cd D:\Atch\Tech\weather-frontend
npm install
npm run dev
```

Open the page shown by Vite (usually http://localhost:5173) and enter latitude and longitude (or run backend on port 3000) to fetch the forecast.
